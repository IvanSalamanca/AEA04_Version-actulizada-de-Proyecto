<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Recurso</title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <header>
        <h1>Editar Recurso: <%= recurso.name %></h1>
    </header>
    <main>
        <section>
            <h2>Formulario de Edición</h2>
            <form action="#" method="POST" class="edit-form">
                <div class="form-group">
                    <label for="name">Nombre:</label>
                    <input type="text" id="name" name="name" value="<%= recurso.name %>">
                </div>

                <div class="form-group">
                    <label for="estado">Estado:</label>
                    <select id="estado" name="estado">
                        <option value="true" <%= recurso.estado ? "selected" : "" %>>Activo</option>
                        <option value="false" <%= !recurso.estado ? "selected" : "" %>>Inactivo</option>
                    </select>
                </div>

                <div class="form-group">
                    <label for="categoria">Categoría:</label>
                    <input type="text" id="categoria" name="categoria" value="<%= recurso.categoria %>">
                </div>

                <button type="submit">Guardar Cambios</button>
            </form>
        </section>
    </main>
</body>

</html>